NODE=$(kubectl get nodes -o jsonpath='{.items[0].metadata.name}')
echo $NODE


kubectl label node "$NODE" family=srvfpj --overwrite

kubectl get node "$NODE" --show-labels | sed 's/,/\n/g'


kubectl -n ms-edi-pix get pods -w
