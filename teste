Check if the files exist in the commit (git ls-files | grep cert/).
If tracked, remove them from the commit (git reset HEAD cert/*.pem).
If already committed, amend/reset the commit (git reset --soft HEAD~1).
