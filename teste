<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Spring Initializr</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1 class="mt-5">Spring Initializr</h1>
        <form th:action="@{/generate}" th:object="${projectMetadata}" method="post">
            <div class="form-group">
                <label for="groupId">Group ID</label>
                <input type="text" id="groupId" th:field="*{groupId}" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="artifactId">Artifact ID</label>
                <input type="text" id="artifactId" th:field="*{artifactId}" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="version">Version</label>
                <input type="text" id="version" th:field="*{version}" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" th:field="*{name}" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                <input type="text" id="description" th:field="*{description}" class="form-control">
            </div>
            <div class="form-group">
                <label for="packaging">Packaging</label>
                <select id="packaging" th:field="*{packaging}" class="form-control">
                    <option value="jar">Jar</option>
                    <option value="war">War</option>
                </select>
            </div>
            <div class="form-group">
                <label for="javaVersion">Java Version</label>
                <select id="javaVersion" th:field="*{javaVersion}" class="form-control">
                    <option value="8">8</option>
                    <option value="11">11</option>
                    <option value="17">17</option>
                </select>
            </div>
            <h3>Dependencies</h3>
            <div th:each="entry : ${mapDependencies}">
                <h4 th:text="${entry.key}">Topic</h4>
                <div th:each="dependency : ${entry.value}">
                    <div class="form-check">
                        <input type="checkbox" th:field="*{dependencies}" th:value="${dependency.artifactId}" class="form-check-input">
                        <label th:for="${dependency.artifactId}" th:text="${dependency.artifactId}" class="form-check-label"></label>
                    </div>
                </div>
            </div>
            <button type="submit" class="btn btn-primary mt-3">Generate Project</button>
        </form>
    </div>
</body>
</html>
