String invalidValue = "abc";

        // Obter um MethodParameter real
        MethodParameter methodParameter = new MethodParameter(
                TestController.class.getMethod("exampleMethod", Integer.class), 0);

        // Criar a exceção MethodArgumentTypeMismatchException
        MethodArgumentTypeMismatchException exception = new MethodArgumentTypeMismatchException(
                invalidValue, Integer.class, "exampleParam", methodParameter, new TypeMismatchException(invalidValue, Integer.class));

        // Lançar a exceção
        throw exception;
