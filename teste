String javaSourcePath = Paths.get("src", "main", "java").toString();
String staticSourcePath = Paths.get("static").toString();

if (sourcePath.toString().contains(javaSourcePath) || sourcePath.toString().contains(staticSourcePath)) {
    return;
}
