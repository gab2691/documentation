1000 * (
  sum by (integration, entityType, generationType, executionID) (
    rate(edi_wrk_integrator_elapsed_millis_sum[5m])
  )
  /
  sum by (integration, entityType, generationType, executionID) (
    rate(edi_wrk_integrator_elapsed_millis_count[5m])
  )
)
