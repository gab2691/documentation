DO
$$
DECLARE
  r RECORD;
BEGIN
  FOR r IN
    SELECT tablename
      FROM pg_tables
     WHERE schemaname = 'dbaprd1'
  LOOP
    EXECUTE format('DROP TABLE dbaprd1.%I CASCADE', r.tablename);
  END LOOP;
END
$$;
